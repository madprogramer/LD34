#pragma strict

var red : int;
var blue : int;
var green : int;

var randomness : int;
var changespeed : float;

var t : float;

function Start () {
	/*red = 0;
	blue =0;
	green=0;
	
	changespeed = 0.025;
	t = 0;
	
	randomness = 4;*/
}

function Update () {

	
	
	/*t+=Time.deltaTime;
	
	if(t >= changespeed)
	{
	red=(red+Random.Range(-randomness,randomness) + 256)%256;
	blue=(blue+Random.Range(-randomness,randomness) + 256 )%256;
	green=(green+Random.Range(-randomness,randomness) + 256)%256;
	t = 0;
	}
	
	gameObject.GetComponent(Camera).backgroundColor = Color(red,blue,green);*/
}